services:
  mysql:
    image: mysql:latest
    container_name: group2
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: mydb
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

    ports:
      - "3306:3306"